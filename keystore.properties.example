# Example only. Do NOT commit `keystore.properties`.
#
# Create `keystore.properties` at the project root (same folder as this file),
# then fill in your real values.
#
# You can also provide these via environment variables or Gradle properties.

RELEASE_STORE_FILE_PATH=my-release-key.jks
RELEASE_STORE_PASS=__REPLACE_WITH_STORE_PASSWORD__
RELEASE_KEY_ALIAS=my-key-alias
RELEASE_KEY_PASS=__REPLACE_WITH_KEY_PASSWORD__

